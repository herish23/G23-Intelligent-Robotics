experiment:
  name: "GridLocalizationTest"
  description: "Replay sensor log with Markov Grid Localization"

# Data paths (Update these paths)
data:
  # --- UPDATE THIS ---
  # Use the full, absolute path to your CSV file
  sensor_data_csv: "C:/Users/Rohit/Desktop/Intelligent Robotics/G23-Intelligent-Robotics/data/sensor_data_clean.csv"
  
  # --- UPDATE THIS ---
  # Path to your map files
  map_pgm_path: "map_data/map.pgm"
  map_yaml_path: "map_data/map.yaml"
  
output_dir: "results"

robot:
  # Initial pose (x_m, y_m, theta_rad)
  # Not used by this grid localizer (starts uniform), but good practice
  initial_pose: [0.0, 0.0, 0.0]

sensors:
  lidar:
    noise_std: 0.1       # Std dev of sensor noise (meters)
    sparsity: 10         # Use 1-in-10 lidar rays to speed up
    max_range: 3.5       # Max range of sensor (meters)
    fov_deg: 360.0       # Field of view
    num_rays: 360        # Total rays in the raw scan
  
  odometry:
    # Not used in this specific script (we use GT motion)
    # but defined for the noise model
    alphas: [0.1, 0.01, 0.1, 0.01]

# Algorithm parameters
localization:
  motion_diffusion_factor: 0.1   # How much to "smear" belief on prediction
  likelihood_sigma: 0.2          # Std dev for the likelihood field (meters)
  likelihood_max_dist: 2.0       # Max distance to calculate likelihood (meters)

# System settings
random_seed: 42
